// Basic script to power the demo page, imports tab modules and instantiates
import Tabs from './tabs';

// Include the styling
require('./_tabs.scss');

$(() => {
  // Document Ready >
  const tabs = new Tabs();
});
